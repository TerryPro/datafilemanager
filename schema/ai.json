{
  "title": "模型配置",
  "description": "为AI配置大模型的相关信息",
  "type": "object",
  "properties": {
    "ai_provider": {
      "title": "AI Provider",
      "description": "Select the AI provider to use",
      "type": "string",
      "enum": ["deepseek", "openai"],
      "default": "deepseek"
    },
    "api_key": {
      "title": "API Key",
      "description": "API Key for the selected provider",
      "type": "string"
    },
    "model_name": {
      "title": "Model Name",
      "description": "Model name to use (e.g., deepseek-chat, gpt-4)",
      "type": "string",
      "default": "deepseek-coder"
    },
    "temperature": {
      "title": "Temperature",
      "description": "Sampling temperature (0.0 to 1.0)",
      "type": "number",
      "minimum": 0,
      "maximum": 1,
      "default": 0.1
    }
  },
  "additionalProperties": false
}
